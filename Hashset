// hashset.add();
// hashset.addAll();
// hashset.clear();
// hashset.remove();

import 'dart:collection';
void main(){
var hashset = new HashSet();

hashset.addAll(["Z", "A", "B", "C"]);
hashset.clear();

print(hashset);
}
